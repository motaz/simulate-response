# simulate-response
Simulate API response
It a web service which is used for testing calling APIs to receive pre-defined JSON/XML response
if calling for example /register with application/json it will read response from register.json file, and for application/xml it will read from register.xml, and
when called with plain/text it will read from register.txt file in the same directory or simulate-response application
